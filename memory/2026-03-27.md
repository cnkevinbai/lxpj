# 2026-03-27 - 代码质量修复

## 修复的问题

### 后端 Java 代码

1. **security-core 模块** (`common/security-core/pom.xml`)
   - 添加 Lombok 依赖
   - 添加 BouncyCastle (bcprov-jdk18on 1.77) - 用于 SM3/SM4 国密算法
   - 添加 Apache Commons Lang3

2. **SM3Util.java**
   - 删除错误的 `java.util.HexAdapter` 导入 (该类不是标准 Java 类)

3. **gateway 模块** (`services/gateway/pom.xml`)
   - 添加 Lombok 依赖
   - 添加 JWT 库 (jjwt-api, jjwt-impl, jjwt-jackson)

4. **jtt808-adapter 模块** (`modules/jtt808-adapter/pom.xml`)
   - 添加 vehicle-access 依赖 (用于 BindingService API)

### 前端 TypeScript 代码

1. **types/binding.ts**
   - 在 `BindDeviceRequest` 接口中添加 `vehicleNo?: string` 属性

2. **stores/bindingStore.ts**
   - 修复导入：从 `@/types/binding` 导入 `BindDeviceRequest`
   - 修复接口定义中的类型引用

3. **tsconfig.json**
   - 添加 `exclude` 配置排除测试文件:
     - `src/**/*.test.ts`
     - `src/**/*.test.tsx`
     - `src/**/__tests__/**`

## 编译/构建结果

- **后端**: 全部 23 个模块编译成功 ✅
- **前端**: iov-portal 构建成功 ✅ (1.85MB)

## 待优化

- 前端包体积过大 (1.85MB)，建议代码分割优化

## 文件变更列表

```
modified: common/security-core/pom.xml
modified: common/security-core/src/main/java/.../SM3Util.java
modified: services/gateway/pom.xml
modified: modules/jtt808-adapter/pom.xml
modified: web/iov-portal/src/types/binding.ts
modified: web/iov-portal/src/stores/bindingStore.ts
modified: web/iov-portal/tsconfig.json
```